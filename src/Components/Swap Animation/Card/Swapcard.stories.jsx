import Card from './Card';

export default {
	title:
		'Dribble Shots/Swap Animation/Final Render',
};

export const SwapCard = (args) => (
	<Card {...args} />
);
SwapCard.storyName = 'Swap Animation Card';
